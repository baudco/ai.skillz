---
model: openai/gpt-5.6-sol
service: opencode
timestamp: 2026-07-23T23:48:34Z
git_ref: wkt/portable_cross_provider_deploy
diff_cmd: git diff HEAD~1..HEAD
---

The user asked to begin real deployment testing after synchronizing the
provider-neutral deployment worktree. A Taken deployment into `dotrc`
succeeded, then an OpenCode-only `commit-msg` deployment failed because Git
refused to check ignore rules beneath a legacy Claude whole-directory
symlink.

> `git diff HEAD~1..HEAD -- scripts/deploy.sh`

Added an effective-ignore predicate that first uses the consumer worktree and
then falls back to a temporary work-tree containing the applicable ignore
files. The fallback stops importing nested ignore files after encountering a
symlink, so external legacy targets cannot influence the result. Deployment
and status now share this check and suppress Git's path-traversal diagnostic.

> `git diff HEAD~1..HEAD -- tests/deploy/test-deploy.sh`

Added a fixture that keeps a legacy Claude `commit-msg` directory link while
deploying the OpenCode hybrid. It verifies deployment succeeds and status does
not report either the Git symlink-traversal error or unhealthy runtime ignores.

Verification completed with all 33 deployment fixtures passing,
`scripts/validate-deployment.sh` reporting zero errors, and `git diff --check`
passing.

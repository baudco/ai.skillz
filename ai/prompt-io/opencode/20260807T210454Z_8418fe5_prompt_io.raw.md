---
model: openai/gpt-5.6-sol
service: opencode
timestamp: 2026-08-07T21:04:54Z
git_ref: 8418fe5
diff_cmd: git diff HEAD~1..HEAD
---

Extracted the multi-commit orchestration previously embedded in `/commit-msg`
into a provider-neutral `/commit-plan` skill while preserving `/commit-msg` as
the authoritative dependency for message style, staged analysis, runtime
artifacts, trailers, and safety rules.

> `git diff HEAD~1..HEAD -- skills/commit-plan skills/commit-msg providers/opencode/commands/commit-plan.md .opencode/commands/commit-plan.md README.md`

The compatibility path redirects direct `/commit-msg` multi-commit requests
once, while `/commit-plan` explicitly ignores only that redirect when loading
the dependency. The plan workflow preserves exact index bytes, materializes and
tests every boundary, generates collision-safe archived messages, restores the
initial index, and renders one command block for the configured shell.

> `git diff HEAD~1..HEAD -- deploy-manifest.conf scripts/deploy.sh scripts/validate-deployment.sh tests/deploy/test-deploy.sh`

Deployment gained first-class skill dependencies. Project and global
deployment now refuse missing `commit-msg`, status and validation report broken
dependencies, and manifest validation rejects indirect dependency cycles.

Validation completed with zero skill/deployment errors, two pre-existing
line-count warnings, 37/37 deployment tests passing, shell syntax checks, and
an inter-skill audit with no actionable findings.

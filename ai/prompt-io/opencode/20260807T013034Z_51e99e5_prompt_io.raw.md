---
model: openai/gpt-5.6-sol
service: opencode
timestamp: 2026-08-07T01:30:34Z
git_ref: 51e99e5
diff_cmd: git diff HEAD~1..HEAD
---

Refined the new `code-review` skill so Python code authored in review
recommendations follows a `py-codestyle` skill selected by the target
repository.

> `git diff HEAD~1..HEAD -- skills/code-review/SKILL.md`

Added the target-selection rule and made it override reviewer-global
`py-codestyle` auto-application for replacement snippets, suggested patches,
and submitted change examples.

> `git diff HEAD~1..HEAD -- skills/code-review/references/python-review.md`

Defined provider-neutral local and remote discovery, trusted-path rules,
registered submodule handling, exact loaded-canonical matching, explicit
authorization for external paths, conflict handling, and fallback formatting.

> `git diff HEAD~1..HEAD -- skills/code-review/references/output-contract.md`

Required Python recommendation blocks to follow the target-selected style
skill while preserving quoted evidence exactly.

> `git diff HEAD~1..HEAD -- skills/code-review/DEPLOY.md`

Documented `py-codestyle` as the optional owner of Python suggestion
formatting.

> `git diff HEAD~1..HEAD -- tests/deploy/test-deploy.sh`

Added contract assertions for target precedence, safe local and remote
discovery, external-path refusal, fallback behavior, evidence preservation,
and severity independence.

Verification completed:

- `bash tests/deploy/test-deploy.sh`: 35 of 35 passed.
- `scripts/validate-skills.sh`: 0 errors and 3 pre-existing line-count
  warnings.
- `scripts/validate-deployment.sh .`: 0 errors.
- `git diff --check`: passed.
- Final inter-skill audit: no semantic findings; its final stale assertion
  finding was corrected and the complete fixture then passed.

No files were staged or committed by this refinement workflow. Existing
unrelated untracked Prompt-IO entries and `bigB/` were not modified.

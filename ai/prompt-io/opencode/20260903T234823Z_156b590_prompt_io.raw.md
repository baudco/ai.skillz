---
model: openai/gpt-5.6-sol
service: opencode
timestamp: 2026-09-03T23:48:23Z
git_ref: 156b590
diff_cmd: git diff HEAD~1..HEAD
---

## Generated changes

> `git diff HEAD~1..HEAD -- skills/commit-plan/SKILL.md`

Tightens the commit-plan orchestration contract around the concrete
first-run failures observed in Piker. The skill now treats `/run-tests`
and its local harness reference as authoritative for complete environment
wrappers, safe scopes, opt-in exclusions, process isolation, ordering and
known outcomes. It requests the broadest documented safe regression gate
instead of synthesizing a raw test-root run.

Shell selection now prefers explicit and known user context over the
harness executor's `$SHELL`. Xonsh output forbids undeclared Python names,
newline continuations and one-shot assertions, and generated commands must
pass a non-mutating target-shell preflight.

Boundary helpers must provide their temporary project with an independent
Git root, preserve fresh-process test boundaries and avoid rerunning a
state-sensitive tier inside a later broad pytest process. The skill also
states explicitly that one atomic boundary is a valid commit plan.

> `git diff HEAD~1..HEAD -- skills/run-tests templates/run-tests`

The composed `/run-tests` contract now owns explicit safe broad regression,
authorization-required command and process-isolation fields. Its local
reference template and examples record those classifications, while missing
metadata falls back conservatively instead of inviting the planner to invent
unsafe coverage or merge separately documented runner processes.

## Verification

- The Piker deployment resolves directly to the edited `ai.skillz`
  source file, and both paths have the same SHA-256.
- `git diff --check` passes in `ai.skillz`.
- No `commit-msg` change is needed; its compatibility redirect and
  dependency ownership are already correct.
- Piker's ignored boundary helper now excludes `tests/ui` from the broad
  gate because both UI files run independently in fresh pytest processes.
- The Piker staged index remained unchanged while its helper was fixed.

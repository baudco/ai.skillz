---
description: Design deterministic tests or author regression coverage.
---

Load the `test-design` skill and follow it completely for the active
repository. Do not author test files unless the user requests that edit, and
do not execute tests without explicit authorization. Delegate every test
command and runtime diagnosis to `run-tests`.

Behavior or test context from the user: $ARGUMENTS

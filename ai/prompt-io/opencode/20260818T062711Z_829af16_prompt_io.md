---
model: gpt-5.6-sol
service: opencode
session: tractor-addr-unpacking-followup
timestamp: 2026-08-18T06:27:11Z
git_ref: 829af16
scope: config
substantive: true
raw_file: 20260818T062711Z_829af16_prompt_io.raw.md
---

## Prompt

The human requested that `/commit-plan` always state the exact
execution directory, especially for linked worktrees, or include the
directory change directly in the runnable plan.

## Response summary

Updated the shared commit-plan contract so command blocks are
self-locating. Every plan must name its repository/worktree root and
branch, then begin with a standalone `cd` to the exact absolute root.

## Files changed

- `skills/commit-plan/SKILL.md` - require explicit roots, branches, and
  directory-change commands in rendered commit plans.

## Human edits

The human identified the missing execution context as a recurring
usability problem and selected an in-band directory change as the most
reliable solution. The agent translated that direction into the skill
rendering and completion contracts; no direct manual source edits were
observed.

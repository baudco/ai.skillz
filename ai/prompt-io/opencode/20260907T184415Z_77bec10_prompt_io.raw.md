---
model: openai/gpt-5.6-sol
service: opencode
timestamp: 2026-09-07T18:44:15Z
git_ref: 77bec10
diff_cmd: git diff HEAD~1..HEAD
---

## Generated changes

> `git diff HEAD~1..HEAD -- skills/commit-plan/scripts/plan-exec.py`

Adds a canonical stateless executor for generated plan specifications. It
validates repository and artifact identity, classifies completed boundaries
from exact single-parent tree history, executes the next pending boundary as
one fail-fast unit and skips completed boundaries before any mutable action.
The executor applies authenticated patches itself, runs project commands in
shared exact-tree clones with independent Git metadata, validates each
command's source-resolution probe under the same environment and constructs
the only permitted editor-backed commit command from an immutable message
snapshot. Raw-object history, canonical OIDs and real-index identity prevent
replacement refs or ambient Git overrides from faking completion.

> `git diff HEAD~1..HEAD -- skills/commit-plan/SKILL.md skills/commit-plan/DEPLOY.md deploy-manifest.conf`

Makes the executor a deployed part of `/commit-plan` and requires generated
sequences to pin their specification, validate exact index transitions and
remain successful after partial or complete execution.

> `git diff HEAD~1..HEAD -- tests/test_commit_plan_exec.py tests/deploy/test-deploy.sh`

Adds executable regressions for first execution, partial/full reruns,
editor-abort recovery, divergence, replacement refs, alternate indexes,
artifact mutation, exact import resolution, runtime symlink escapes, relative
search paths and non-blocking rejection of FIFO artifacts. Deployment tests
also require and execute the helper suite.

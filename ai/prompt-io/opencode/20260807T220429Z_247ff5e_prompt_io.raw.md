---
model: openai/gpt-5.6-sol
service: opencode
timestamp: 2026-08-07T22:04:29Z
git_ref: 247ff5e
diff_cmd: git diff HEAD~1..HEAD
---

Updated provider-aware skill deployment so selecting OpenCode installs every
OpenCode command whose manifest dependency names the deployed skill.

> `git diff HEAD~1..HEAD -- scripts/deploy.sh tests/deploy/test-deploy.sh`

The combined skill/command operation preflights all destinations, command
sources, dependencies, ignore rules, and staging constraints before mutation.
`--provider all` installs OpenCode shims only on the OpenCode side;
`--no-command` provides an explicit skill-only escape hatch; the standalone
`command` subcommand remains available for repair and migration.

Manifest validation now rejects missing and template-only command dependencies.
Tests cover single-skill and `all` automation, provider isolation, local and
portable staging, exact ignore updates, idempotence, opt-out, explicit repair,
and zero-mutation command conflicts.

> `git diff HEAD~1..HEAD -- README.md skills/code-review/DEPLOY.md skills/code-review-changes/DEPLOY.md skills/commit-msg/DEPLOY.md skills/commit-plan/DEPLOY.md skills/opencode-cleaning/DEPLOY.md skills/run-tests/DEPLOY.md skills/taken-export/DEPLOY.md`

Deployment documentation now describes automatic OpenCode command shims and
the explicit repair/opt-out paths.

Validation completed with 38/38 deployment tests passing, zero skill or
deployment errors, two pre-existing line-count warnings, shell syntax checks,
and clean whitespace diagnostics.

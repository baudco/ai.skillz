---
model: openai/gpt-5.6-sol
service: opencode
timestamp: 2026-07-24T02:03:36Z
git_ref: wkt/portable_cross_provider_deploy
diff_cmd: git diff HEAD~1..HEAD
---

The user asked to expand the `ai.skillz` self-host deployment across all
relevant skills. Deploying the Claude catalog succeeded, but running command
deployment for both providers rewrote tracked relative OpenCode self-host
links as ignored absolute consumer links. The deployment validator reported
all three source-repository commands as non-canonical.

> `git diff HEAD~1..HEAD -- scripts/deploy.sh`

Preserve a direct command destination when the target is the deployment
source, the destination is a tracked symlink, and it resolves to the canonical
command asset. Reconcile any stale managed ignore block, preserve runtime
ignores, and return without rewriting the link.

> `git diff HEAD~1..HEAD -- tests/deploy/test-deploy.sh`

Exercise OpenCode `command all` against a cloned source repository. Verify the
tracked relative command target remains unchanged, the command tree has no Git
diff, and the command is not ignored.

All 33 deployment fixtures passed, deployment validation reported zero
errors, and `git diff --check` passed. The real source-repository links were
restored to their tracked relative targets before validation.

---
model: gpt-5.6-sol
service: opencode
timestamp: 2026-08-18T06:27:11Z
git_ref: 829af16
diff_cmd: git diff HEAD~1..HEAD
---

# Raw output - self-locating commit plans

The human observed that a commit plan for a linked worktree did not
make its execution directory sufficiently prominent. They requested
that `/commit-plan` always identify where a plan must run, preferably
by changing to that directory within the runnable command sequence.

## Generated change

> `git diff HEAD~1..HEAD -- skills/commit-plan/SKILL.md`

Required every commit-plan command block to begin with a standalone,
shell-correct `cd` to the absolute `git rev-parse --show-toplevel`
path. Plans spanning repositories must change directories before each
different boundary. The report and completion gate now also require the
exact root and branch.

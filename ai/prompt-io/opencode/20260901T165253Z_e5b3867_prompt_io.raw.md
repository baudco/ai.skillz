---
model: openai/gpt-5.6-sol
service: opencode
timestamp: 2026-09-01T16:52:53Z
git_ref: e5b3867
diff_cmd: git diff HEAD~1..HEAD
---

The deferred `commit-plan` reconstruction passed its test and deployment
validation, then an inter-skill audit found five state-machine gaps. Correct
all findings before treating the experiment as ready.

Use one coherent contract rather than isolated prose patches:

- allow receipt-less planning without inventing discovery policy by keeping a
  standalone, worktree-private plan-state artifact; mirror that state into a
  receipt extension only when a valid exact-key receipt already exists;
- define the `extensions.commit-plan` schema, stable IDs, lifecycle states,
  checks and boundary-owned reply records in a deployed schema asset;
- execute every boundary command against an isolated work tree as well as a
  private index so later-boundary content cannot affect checks or hooks;
- preserve and scope review/regression context across all planned boundaries
  rather than allowing the first message to consume shared context;
- make successful boundary completion and rerun recovery generate and report
  local reply candidates, while retaining separate exact publication approval;
- add executable schema/fixture tests and retain the existing deployment
  contract checks.

Do not run project checks during planning unless explicitly requested. Do not
weaken commit, network, review-publication or task-state authorization gates.

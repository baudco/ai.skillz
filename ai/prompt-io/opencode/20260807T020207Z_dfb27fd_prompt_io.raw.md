---
model: openai/gpt-5.6-sol
service: opencode
timestamp: 2026-08-07T02:02:07Z
git_ref: dfb27fd
diff_cmd: git diff HEAD~1..HEAD
---

The requested implementation adds a provider-neutral `harness-perf` skill
for diagnosing CPU, memory, latency, hangs, render loops, watcher churn,
subagent overhead, and session-scale effects in AI coding harnesses.

> `git diff HEAD~1..HEAD -- skills/harness-perf/SKILL.md`

The generated skill uses a measurement-first workflow: establish safety and
comparators, inventory process/session scale, attribute work by process and
thread, control self-observation bias, run one-variable A/B experiments,
classify the bottleneck, correlate exact versions with upstream evidence,
and produce an actionable report. It explicitly prevents destructive process,
database, config, and kernel changes without user approval.

> `git diff HEAD~1..HEAD -- skills/harness-perf/references/opencode.md`

The generated OpenCode reference preserves the 2026-08-06 investigation in
detail: OpenCode version, process and session scale, database metrics, thread
attribution, animation-enabled versus disabled behavior, source-level spinner
correlation, related upstream memory reports, transient result-ingestion cost,
tooling limitations, mitigation order, and follow-up experiments.

> `git diff HEAD~1..HEAD -- skills/harness-perf/DEPLOY.md`

The deployment guide documents generic provider deployment, restart
requirements, optional sanitized report storage, and prerequisites.

> `git diff HEAD~1..HEAD -- deploy-manifest.conf README.md tests/deploy/test-deploy.sh`

The skill is registered as a generic deployable skill and listed in the
repository skill catalog. The deployment fixture overlays the new uncommitted
skill into its synthetic source checkout so manifest validation exercises the
same source tree as the working copy.

Validation completed successfully: skill validation reported zero errors and
three pre-existing line-count warnings, deployment validation reported zero
errors, the deployment regression suite passed all 35 tests, and
`opencode debug skill` discovered `harness-perf` from its canonical path.

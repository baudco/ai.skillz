# Test Harness Reference: {{ project_name }}

This repository-local file supplements the canonical `/run-tests` skill.
Keep project commands, paths, fixtures, and expected outcomes here; keep the
shared execution and safety workflow in the canonical `SKILL.md`.

## Project And Environment

- Project: `{{ project_name }}`
- Package/import name: `{{ import_name }}`
- Test root: `{{ test_root }}`
- Preferred virtual environment: `{{ venv_name }}`
- Required shell or environment setup:

```text
{{ environment_setup }}
```

## Commands

Base test command:

```text
{{ base_test_command }}
```

Import and worktree-resolution check:

```text
{{ import_check_command }}
```

Collection check:

```text
{{ collection_check_command }}
```

Default flags that may be added unless the user overrides them:

{{ default_flags }}

## Scope And Path Resolution

Describe how bare filenames, directories, node IDs, keyword expressions,
and full-suite requests map into `{{ test_root }}`. Record any suites that
must not run by default because they require credentials, network access,
GUI services, containers, or special hardware.

## Project-Specific Flags And Backend Matrix

{{ custom_flags }}

Document supported combinations and the commands that provision each
backend. Do not list planned but unavailable options as runnable.

## Fixture Invariants

{{ fixture_invariants }}

Record source data, temporary-copy behavior, environment restoration, and
other invariants whose violation can create misleading failures.

## Test Layout

```text
{{ test_layout }}
```

## Change-To-Test Mapping

{{ change_test_mapping }}

Prefer deterministic unit tests first, then the smallest relevant
integration/backend matrix.

## Quick Checks

```text
{{ quick_check_commands }}
```

## Known Outcomes

{{ known_outcomes }}

List exact node IDs, conditions, and expected signatures. Do not classify a
failure from only an exception class such as `TooSlowError`.

## Tractor Runtime Notes

{{ tractor_runtime_notes }}

State whether the suite loads `tractor._testing.pytest`, whether registry
addresses are fixed or session-unique, which capture modes are supported,
and whether a repository-local cleanup command exists.

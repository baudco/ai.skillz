---
description: Manage local issue and pull-request files across forge backends.
---

Load the `gish` skill and follow it completely. Use the requested operation,
backend, repository, and target as its arguments. Keep local-file preparation
separate from remote publication; this invocation authorizes a remote write
only when the user explicitly requests that exact write.

Forge operation from the user: $ARGUMENTS

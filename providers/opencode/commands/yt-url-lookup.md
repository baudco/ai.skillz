---
description: Resolve local audio files to YouTube source URLs and build an index.
---

Load the `yt-url-lookup` skill and follow it completely for the requested
directory and rescan mode. Preserve existing lookup evidence unless `--rescan`
was supplied, report confidence honestly, and write only the workflow's local
index artifacts.

Lookup request from the user: $ARGUMENTS

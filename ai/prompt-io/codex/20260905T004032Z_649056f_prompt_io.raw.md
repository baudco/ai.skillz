---
model: GPT-6 (exact runtime model identifier unavailable)
service: codex
timestamp: 2026-09-05T00:40:32.120143+00:00
git_ref: 649056f
diff_cmd: git diff 649056f
---

## Code output (diff-ref mode)

The changes are uncommitted in `wkt/codex_shared_skills`. These pointers
cover working-tree edits; newly created files enter the Git diff when
staged. No human file edits have been observed during implementation.

> `git diff 649056f -- AGENTS.md`

> `git diff 649056f -- README.md`

> `git diff 649056f -- docs/codex-support.md`

> `git diff 649056f -- scripts/deploy.sh`

> `git diff 649056f -- scripts/validate-deployment.sh`

> `git diff 649056f -- skills/close-wkt/agents/openai.yaml`

> `git diff 649056f -- skills/code-nav-refs/SKILL.md`

> `git diff 649056f -- skills/code-review-changes/agents/openai.yaml`

> `git diff 649056f -- skills/code-review/DEPLOY.md`

> `git diff 649056f -- skills/commit-plan/DEPLOY.md`

> `git diff 649056f -- skills/commit-plan/SKILL.md`

> `git diff 649056f -- skills/dep-supersede-scan/agents/openai.yaml`

> `git diff 649056f -- skills/inter-skill-review/SKILL.md`

> `git diff 649056f -- skills/open-wkt/SKILL.md`

> `git diff 649056f -- skills/plan-io/SKILL.md`

> `git diff 649056f -- skills/plan-io/agents/openai.yaml`

> `git diff 649056f -- skills/pr-msg/agents/openai.yaml`

> `git diff 649056f -- skills/prompt-io/SKILL.md`

> `git diff 649056f -- skills/py-codestyle/agents/openai.yaml`

> `git diff 649056f -- skills/resolve-conflicts/agents/openai.yaml`

> `git diff 649056f -- skills/yt-url-lookup/SKILL.md`

> `git diff 649056f -- skills/yt-url-lookup/agents/openai.yaml`

> `git diff 649056f -- tests/deploy/shared-cases.sh`

> `git diff 649056f -- tests/deploy/test-codex.py`

> `git diff 649056f -- tests/deploy/test-deploy.sh`

The shared deployment selector reuses the existing deployer. Shared
skills use whole-directory links so Codex discovers legacy hybrid
skills too. Runtime state stays at its existing repository-owned
paths. Optional Codex metadata preserves existing manual invocation
policy. Tests cover links, source ownership, dependencies, migration,
cloning, and the installed Codex skill registry.

## Verbatim progress output

We don’t need a Codex-specific worktree opener. The temporary
helper just carried out this repo’s existing `/open-wkt` procedure;
it isn’t being added as a new skill or required tool.

Codex should use the same `open-wkt` skill as other harnesses.
The changes belong in shared skill discovery and deployment, with
small adapters only where a harness requires them.

The Codex loader check caught a real gap: it discovers directory
symlinks, but skips the existing hybrid layout’s symlinked
`SKILL.md` files. That leaves `commit-msg`, `pr-msg`, and `run-tests`
missing, so `commit-plan` would lack its dependency.

I’m adjusting shared deployment to link whole skill directories.
Repository-owned runtime state will stay at its existing paths,
outside those shared source links.

Codex now discovers all 21 skills, including `commit-msg`,
`pr-msg`, and `run-tests`. Its rendered prompt also loads the repo’s
`AGENTS.md` and respects the preserved manual-only skill policies.

## Bootstrap output (diff-ref mode)

> `git diff 649056f -- .agents/skills tests/deploy/shared-cases.sh`

Install shared relative discovery links and make the isolated
self-hosting test fixture tolerate an already deployed source tree.

Verbatim loader result:

```text
PASS: Codex discovers 21 shared skills
```

## Shared documentation output (diff-ref mode)

> `git diff 649056f -- README.md docs skills/code-review/DEPLOY.md skills/commit-plan/DEPLOY.md`

Make shared deployment the documentation entry point. Keep Codex
adapter details separate and describe what has actually been validated
for each harness, without implying untested integrations are complete.

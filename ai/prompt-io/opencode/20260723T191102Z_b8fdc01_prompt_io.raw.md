---
model: openai/gpt-5.6-sol
service: opencode
timestamp: 2026-07-23T19:11:02Z
git_ref: wkt/portable_cross_provider_deploy
diff_cmd: git diff b8fdc01..HEAD
---

The user asked whether all `ai.skillz` deployment scripts had been ported for
OpenCode and cross-harness use. After learning that the provider-neutral
deployer remained planned but unimplemented, the user asked to implement the
entire plan in an `/open-wkt` worktree.

The work was isolated at `.claude/wkts/portable_cross_provider_deploy` on
branch `wkt/portable_cross_provider_deploy`. The root checkout's dirty Taken
work was not modified.

> `git diff b8fdc01..HEAD -- scripts/deploy.sh deploy-manifest.conf gitignore-patterns.conf`

Implemented a manifest-driven provider-aware deployment CLI with
`.ai/ai.skillz` local and submodule anchors, Claude/OpenCode/all provider
selection, direct-link compatibility, exact staging, bounded ignore blocks,
safe hybrid deployment, update, status, migration, and strict path ownership.

> `git diff b8fdc01..HEAD -- scripts/validate-deployment.sh tests/deploy/test-deploy.sh`

Added index-aware deployment validation and 27 fixtures covering portable and
direct anchors, provider coexistence, hybrid state, dirty index preservation,
legacy migration, copied commands, JSON/JSONC config inspection, external
path containment, update behavior, and OpenCode discovery.

> `git diff b8fdc01..HEAD -- providers/opencode/commands/commit-msg.md .opencode/commands/commit-msg.md`

Added the canonical OpenCode `/commit-msg` command asset and made the source
repository consume it through a portable relative symlink.

> `git diff b8fdc01..HEAD -- README.md commands/ skills/ docs/deployment-consumer-inventory.md`

Updated active deployment guides for the provider-neutral contract and added
a read-only inventory with explicit rollout results for `lns`, `tractor`,
`piker`, `modden`, `ai.reply`, and `ai.skillz`. No consumer was mutated.

Validation passed with 27 deployment fixtures, zero deployment-validator
errors, zero skill-validator errors, Bash syntax checks, OpenCode debug
discovery, and `git diff --check`. Three pre-existing skill-length warnings
remain. The historical deployment plan and its task markers were untouched.

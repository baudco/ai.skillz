---
model: openai/gpt-5.6-sol
service: opencode
timestamp: 2026-08-14T16:59:48Z
git_ref: wkt/test_design_skill
diff_cmd: git diff HEAD~1..HEAD
---

The user transferred a Tractor-agent handoff requesting a canonical
`test-design` skill in `ai.skillz`.

The requested ownership split is one-way: `test-design` owns failure models,
proof obligations, test-layer choice, deterministic orchestration, regression
test authoring, mock justification, and proof gaps; `run-tests` owns concrete
environments, commands, executable scopes, matrices, timeouts, execution,
runtime diagnosis, and cleanup.

The generated implementation adds the new skill and OpenCode command,
registers `test-design -> run-tests` in the deployment manifest, updates
`run-tests`, `code-review`, and the run-tests harness template to preserve the
boundary, and extends deployment/discovery tests. It avoids a shared runtime
artifact because direct in-session delegation has no lifecycle or worktree
ambiguity.

Generated file changes are represented by:

> `git diff HEAD~1..HEAD -- skills/test-design deploy-manifest.conf providers/opencode/commands/test-design.md`

> `git diff HEAD~1..HEAD -- skills/run-tests skills/code-review templates/run-tests/SKILL.md.j2`

> `git diff HEAD~1..HEAD -- tests/deploy/test-deploy.sh README.md docs/deployment-consumer-inventory.md`

The design prefers the cheapest test layer that exercises every material
failure boundary. Concurrency, IPC, cancellation, transport, and process
lifecycle tests default to real schedulers, channels, processes, and cleanup
paths. Events and barriers provide ordering; sleeps remain timeout guards.
Mocks require an explicit account of the replaced seam, retained behavior,
lost behavior, and residual uncertainty.

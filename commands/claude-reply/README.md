# `claude-reply` moved to `ai.reply`

The Neovim quote-reply plugin now lives in the standalone local repo:

```text
~/repos/ai.reply
```

Use `~/repos/ai.reply/README.md` and `~/repos/ai.reply/DEPLOY.md` for
the current implementation, docs, and local lazy.nvim deployment.

This directory remains as a pointer for historical links from
`ai.skillz` plans and notes.

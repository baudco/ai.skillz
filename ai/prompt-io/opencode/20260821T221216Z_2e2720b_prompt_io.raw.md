---
model: openai/gpt-5.6-sol
service: opencode
timestamp: 2026-08-21T22:12:16Z
git_ref: 2e2720b
diff_cmd: git diff HEAD~1..HEAD
---

## Prompt

Rename `code-location-refs` to the shorter `code-nav-refs` name so it pairs
more clearly with FapNav. Apply the rename across `ai.skillz`, update deployed
consumers, and do not retain a compatibility alias unless real consumers
require one.

## Response

Renamed the provider-neutral skill directory and frontmatter, then updated its
deployment guide, manifest registration, catalog entry, and deployment test
fixture.

> `git diff HEAD~1..HEAD -- skills/code-nav-refs deploy-manifest.conf README.md tests/deploy/test-deploy.sh`

Scanned repository-local Claude and OpenCode skill destinations and found no
deployed consumers. The global `~/.claude/skills` directory is a symlink to the
source `ai.skillz/skills` tree, so the source rename updated the global skill
atomically. Removed the empty old-name directory without adding an alias.

---
model: gpt-5.6-sol (openai)
service: opencode
timestamp: 2026-07-27T22:29:52Z
git_ref: 3f3c599
diff_cmd: git diff HEAD~1..HEAD
---

> `git diff HEAD~1..HEAD -- skills/py-codestyle/SKILL.md`

Added namespace-qualified technical prose rules for Python comments and
docstrings. The guidance requires exact declaring symbols for registries,
tables, mappings, indexes, and caches; asks instance-specific prose to
name both the concrete expression and class field; requires mutation
descriptions to name the method and affected fields; and optimizes
regression documentation for editor symbol lookup.

The rule was extracted from modden shared-subwindow lifecycle review,
where phrases such as "global SID table", "this mapping", "graphical
cache", and "parent-local indexes" obscured `Runspace._sid2spawn`,
`Spawn.sub_spawns`, `Spawn.win_ids2sub_sids`, and `Wks.wins`.

`git diff --check -- skills/py-codestyle/SKILL.md` passed. Existing
uncommitted edits and prompt logs in the `ai.skillz` repository were
left untouched.

---
description: Build a complete multi-commit plan with project-style messages.
---

Load the `commit-plan` skill and follow it completely. It must load the
repository's deployed `commit-msg` skill as its message-generation dependency
without re-entering the compatibility redirect. Generate every message and
exact boundary, restore the starting index, and never commit automatically.

Boundary guidance from the user: $ARGUMENTS

---
model: openai/gpt-5.6-sol
service: opencode
session: ses_faae5c497ffeS0ofaTEfbRo2LP
timestamp: 2026-09-07T18:44:15Z
git_ref: 77bec10
scope: code
substantive: true
raw_file: 20260907T184415Z_77bec10_prompt_io.raw.md
---

## Prompt

Follow up the Piker `/commit-plan` fix so a rendered sequence remains safe
to rerun after partial or complete execution. The human clarified that this
was the central purpose of the work and authorized implementing the missing
end-to-end behavior.

## Response summary

Adds a receipt-free execution protocol which classifies completed commit
boundaries from their exact parent/tree chain. Completed boundaries become
successful no-ops before staging, checks, editor or hooks run; pending
boundaries execute fail-fast and refuse divergence.

The canonical executor owns authenticated patch application, structural
checks, independent exact-tree clones, staged review and the sole permitted
editor-backed commit form. It rejects ambient Git redirection and replacement
refs, validates canonical object IDs, binds project checks to same-environment
source-resolution probes and authenticates runtime artifacts from immutable
byte snapshots.

The change also adds behavioral coverage for initial execution, partial and
complete reruns, editor interruption, unrelated-history refusal, alternate
indexes, replacement refs, artifact mutation, import escape, runtime symlinks,
relative search paths and FIFO artifacts.

## Files changed

- `skills/commit-plan/scripts/plan-exec.py` - canonical receipt-free plan
  executor and exact-history classifier.
- `skills/commit-plan/SKILL.md` - require the canonical executor and
  rerunnable boundary protocol.
- `skills/commit-plan/DEPLOY.md` - document the deployed executor asset.
- `deploy-manifest.conf` - deploy the executor with `commit-plan`.
- `tests/test_commit_plan_exec.py` - exercise rerun and divergence behavior.
- `tests/deploy/test-deploy.sh` - cover deployment and workflow contracts.

## Human edits

The human identified that the first patch addressed parser and one-shot
assertion failures without guaranteeing that the whole command sequence
could be rerun after a commit changed `HEAD`. They made successful no-op
behavior after partial and complete execution the acceptance criterion and
authorized this focused follow-up.

---
description: Preview and remove stale OpenCode fork sessions safely.
---

Load the `opencode-cleaning` skill and follow it completely for the current
repository. Always run and show a dry-run preview first. This initial command
never authorizes deletion. Apply a preview token only after a follow-up human
message explicitly approves that exact candidate list and token.

Cleaning options from the user: $ARGUMENTS

---
description: Analyze and reconcile active Git conflict paths safely.
---

Load the `resolve-conflicts` skill and follow it completely for the active Git
operation and optional file path. Resolve requested file content only after
establishing authorization provenance. Do not stage, continue, skip, or abort
the operation unless the user separately authorizes that exact mutation.

Conflict scope from the user: $ARGUMENTS

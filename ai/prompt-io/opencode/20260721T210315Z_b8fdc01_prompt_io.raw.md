---
model: openai/gpt-5.6-sol
service: opencode
timestamp: 2026-07-21T21:03:15Z
git_ref: run_tests_skill_formalize
diff_cmd: git diff b8fdc01..HEAD
---

The base-anchored diff pointers below resolve the full planned commit series
once it is committed. Before commit, inspect tracked changes with
`git diff HEAD` and untracked paths with `git status` plus direct file reads.

The user confirmed that cross-repository `/run-tests` generalization was
outstanding and asked to begin it. The user also asked whether OpenCode's
`/taken-export` command placement required a gitignore-template change, then
required all `/run-tests` work to use `/open-wkt` on the exact branch
`run_tests_skill_formalize` without a `wkt/` prefix.

The branch was moved from the main checkout into
`.claude/wkts/run_tests_skill_formalize` while preserving the exact branch
name. The main checkout returned to `taken_export_skill`.

> `git diff b8fdc01..HEAD -- skills/run-tests/SKILL.md`

Added the cross-repository base workflow. It resolves a repository-local
`test-harness-reference.md`, validates worktree-local environments, preserves
targeted scope, directly inspects pytest last-failed JSON, and conditionally
diagnoses Tractor runtime leaks and capture-pipe hangs without assuming a
fixed registry or repository-local reaper.

> `git diff b8fdc01..HEAD -- scripts/deploy.sh`

Changed `run-tests` deployment from a template-only no-op to a hybrid
canonical `SKILL.md` link plus local override. Added safety for legacy
whole-directory symlinks, source validation before mutation, malformed
override rejection, refusal to overwrite regular local skills, idempotent
override preservation, and method-sensitive absolute-link ignores.

> `git diff b8fdc01..HEAD -- templates/run-tests/SKILL.md.j2`

Converted the full-skill template into a repository harness-reference
template with explicit environment, command, fixture, mapping, backend, known
outcome, and Tractor runtime sections.

> `git diff b8fdc01..HEAD -- skills/run-tests/DEPLOY.md skills/run-tests/ROADMAP.md skills/run-tests/references/tractor-example.md skills/run-tests/references/modden-example.md`

Documented hybrid deployment and migration, updated roadmap facts without
changing its TODO state, reduced the Tractor example to override-only data,
and added a Modden example covering current i3/Sway behavior and hermetic
fixture invariants.

> `git diff b8fdc01..HEAD -- .opencode/commands/run-tests.md .opencode/commands/taken-export.md skills/taken-export/DEPLOY.md gitignore-patterns.conf README.md`

Added tracked OpenCode slash-command shims and documented why command shims
must remain tracked. Kept deployment links out of the static ignore manifest
because absolute development links should be ignored while portable relative
links should be tracked.

> `git diff b8fdc01..HEAD -- scripts/test-run-tests-deploy.sh`

Added reproducible fixtures for both link methods, legacy migration safety,
missing-submodule preservation, local-skill refusal, malformed override
rejection, idempotence, ignore behavior, and status reporting.

Validation included skill-schema checks, shell syntax, whitespace checks,
the persistent deployment fixture suite, and review of Tractor cleanup scope.

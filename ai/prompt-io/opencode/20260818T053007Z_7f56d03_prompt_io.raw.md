---
model: gpt-5.6-sol
service: opencode
timestamp: 2026-08-18T05:30:07Z
git_ref: 7f56d03
diff_cmd: git diff HEAD~1..HEAD
---

# Raw output - honest human contribution accounting

The human identified that a tractor Prompt-IO entry incorrectly said
there were no human edits even though several review turns materially
changed the patch. They then observed that historical Prompt-IO entries
may have used the same overly narrow interpretation and requested more
honest accounting going forward.

The deployed Prompt-IO skill was resolved to its source in
`/home/goodboy/repos/ai.skillz`, as the human expected.

## Generated change

> `git diff HEAD~1..HEAD -- skills/prompt-io/SKILL.md`

Expanded the `## Human edits` contract to include iterative review,
corrections, rejected approaches, requested deletions, architecture and
scope decisions, and acceptance criteria. It now requires attribution
to the human even when an AI agent applies the resulting source lines,
and permits `None` only when the interaction has no material follow-up
revision.

## Repository observation

A scan of existing tractor Prompt-IO records found several entries
using `None` or `committed as generated`. Those historical records were
not rewritten without checking their source interactions.

## Retroactive audit approach

A trustworthy audit can inventory every main Prompt-IO entry, resolve
its source session from service/session/timestamp metadata, compare
follow-up turns and review-driven code changes, and emit append-only
correction records for entries whose human contribution was
understated. Entries without recoverable source evidence should remain
unchanged and be reported as unverifiable rather than guessed.

---
model: openai/gpt-5.6-sol
service: opencode
timestamp: 2026-07-23T20:37:57Z
git_ref: wkt/portable_cross_provider_deploy
diff_cmd: git diff HEAD
---

The user asked to continue after reviewing the completed provider-neutral
deployment worktree and to synchronize it with the concurrent Taken and
`run-tests` work.

The integration keeps `.opencode/commands/` as a provider discovery tree and
uses `providers/opencode/commands/` as the canonical command source. Local
deployments use ignored absolute links, while portable submodule deployments
use trackable relative links. The source repository self-hosts through
relative links to the same canonical provider assets.

> `git diff HEAD -- deploy-manifest.conf scripts/deploy.sh`

The manifest and deployment engine are updated for the shared `run-tests`
base, provider-owned Taken and Run OpenCode commands, method-sensitive local
versus portable links, migration, status, and a usable default repository URL.

> `git diff HEAD -- providers/opencode/commands .opencode/commands`

Canonical OpenCode command shims and source-repository discovery links are
added for `taken-export` and `run-tests`.

> `git diff HEAD -- skills/run-tests templates/run-tests README.md gitignore-patterns.conf`

The committed `run_tests_skill_formalize` base-plus-local-reference model is
integrated without changing the human-owned roadmap task state. Deployment
documentation and runtime export ignores are aligned with the provider-neutral
policy.

> `git diff HEAD -- tests/deploy/test-deploy.sh scripts/validate-deployment.sh`

Fixtures and validation cover canonical command assets, local ignored links,
portable relative links, run-tests hybrid state preservation, and clean-clone
execution.

Consumer repositories remain untouched until this integrated fixture suite
passes. No files are staged or committed.

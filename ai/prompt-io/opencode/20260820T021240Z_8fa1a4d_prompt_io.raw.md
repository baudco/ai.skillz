---
model: openai/gpt-5.6-sol
service: opencode
timestamp: 2026-08-20T02:12:40Z
git_ref: 8fa1a4d
diff_cmd: git diff HEAD~1..HEAD
---

## Prompt

A repository-relative citation failed because the cited file was in another
worktree rather than the editor's active checkout. Require agents to emit
absolute paths for files outside the active worktree while retaining relative
paths for verified files inside it.

## Response

Updated `code-location-refs` to establish the active root with
`git rev-parse --show-toplevel`, verify every relative citation from that root,
and use absolute paths for files in another worktree or repository checkout.
The rule rejects incidental tool-call working directories as citation roots.

> `git diff HEAD -- skills/code-location-refs/SKILL.md`

All 39 deployment tests passed.

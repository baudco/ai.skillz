---
model: claude-opus-4-6
service: claude-code
session: f5358101-1a37-4249-9b90-e064f91a0c0b
timestamp: 2026-04-15T00:26:41Z
git_ref: 4b2f2e8
scope: code
substantive: true
raw_file: 20260415T002641Z_4b2f2e8_prompt_io.raw.md
---

## Prompt

Port the commit-history analysis tooling from
`~/repos/claude_wks/` (`analyze_commits.py` +
`commit_gen.py`) into `ai.skillz/scripts/` as a
self-contained `generate-style-guide.py`. The
`/commit-msg` DEPLOY.md references this script but
it doesn't exist yet. The script should handle the
full pipeline: git log export, statistical analysis,
and style guide markdown rendering — no manual
export step, no external deps.

Also: add `/open-wkt` generated files (`.claude/wkts/`
and `claude_wkts` symlink) to `.gitignore`.

## Response summary

Create `scripts/generate-style-guide.py` by porting
all 6 analysis functions from the `claude_wks`
original, adding built-in `git log` export, CLI arg
parsing, and a `render_style_guide()` function that
outputs a complete `style-guide-reference.md`
matching the per-repo guide format. Update DEPLOY.md
to reference the real script and `deploy-skill.sh`
to print a post-deploy hint with the exact command.

## Files changed

- `scripts/generate-style-guide.py` — new CLI
  script, ported + refactored from `claude_wks`
- `skills/commit-msg/DEPLOY.md` — fix Option A
  to use real script flags, note Option B structure
- `scripts/deploy-skill.sh` — print style guide
  generation command after deploying `commit-msg`
- `.gitignore` — add `.claude/wkts/` and
  `claude_wkts` entries

## Human edits

None — committed as generated.

---
model: openai/gpt-5.6-sol
service: opencode
session: ses_faae5c497ffeS0ofaTEfbRo2LP
timestamp: 2026-09-03T23:48:23Z
git_ref: 156b590
scope: config
substantive: true
raw_file: 20260903T234823Z_156b590_prompt_io.raw.md
---

## Prompt

Distill the minimum instruction set needed for new agents to generate a
correct `/commit-plan` on the first try, comparing it against the deployed
skill in `ai.skillz/`. Account for the observed failures: wrong shell
selection, invalid xonsh using an undeclared Python module, non-idempotent
assertions, missing optional dependencies, accidental live/configured test
execution, a temporary archive resolving the outer Git root, and same-process
fixture contamination when UI tests ran after actor tests.

## Response summary

Refines the existing deployed `commit-plan` skill rather than layering a
second instruction set over it. The final change makes `/run-tests` a formal
dependency, selects only documented safe regression scopes, resolves and
preflights the actual command parser, and preserves Git-root and fresh-process
isolation in exact-boundary helpers. One-boundary plans are explicitly valid.

## Files changed

- `skills/commit-plan/SKILL.md` - correct test selection, shell rendering,
  boundary helper isolation and one-boundary semantics.
- `deploy-manifest.conf` - declare the required `run-tests` dependency.
- `skills/commit-plan/DEPLOY.md` - document both required companions and the
  safe broad-regression contract.
- `skills/commit-msg/DEPLOY.md` - document the composed deployment order.
- `providers/opencode/commands/commit-plan.md` - require both dependencies.
- `skills/run-tests/SKILL.md` and `skills/run-tests/DEPLOY.md` - make safe
  broad sequences, opt-in commands and process isolation explicit catalog
  fields with conservative fallback rules.
- `templates/run-tests/SKILL.md.j2` and both examples - capture those fields
  in repository-local harness references.
- `tests/deploy/test-deploy.sh` - cover rendering and dependency contracts.

## Human edits

The human supplied every concrete failure that narrowed this change: they
required xonsh despite the harness `$SHELL`, rejected undefined
`subprocess` use and one-shot assertions, reported optional dependency
collection failures and slow live tests, and provided the final broad-run
trace proving UI state contamination after `tmpconfdir`-using actor tests.
They explicitly requested the minimum possible instruction set and required
comparison against the deployed `ai.skillz` source. A follow-up review found
that the initial patch left `run-tests` undeclared, retained stale tests, used
an underspecified preflight and had a transient diff pointer. The human directed
moving the work off the Tuicr checkout and completing those fixes in a clean
main-based worktree.

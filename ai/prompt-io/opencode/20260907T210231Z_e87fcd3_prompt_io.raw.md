---
model: openai/gpt-5.6-sol
service: opencode
timestamp: 2026-09-07T21:02:31Z
git_ref: e87fcd3
diff_cmd: git diff HEAD~1..HEAD
---

Deploy the provider-neutral `.agents/skills` tree for Codex first in
`tractor`, `piker`, `ai.reply`, `taken`, and `modden`. Keep runtime
migration secondary.

Do not reject archived patch and helper-script files merely because
their extensions could indicate unfinished work. Preserve those files
inside provider-neutral runtime state without changing their bytes or
their source copies. Files which capture tracked Git content do not
need semantic interpretation during migration.

Keep reusable skill implementation in canonical skill directories,
but do not mistake one-run archived helpers for reusable source.

> `git diff HEAD~1..HEAD -- scripts/workflow-state.py`

Remove extension-based migration blockers while retaining existing
digest, destination-conflict, symlink, and rollback protections.

> `git diff HEAD~1..HEAD -- tests/deploy/test-workflow-state.py`

Replace the helper-refusal assertion with regression coverage proving
that patch and executable helper archives migrate byte-for-byte and
preserve their file modes.

> `git diff HEAD~1..HEAD -- docs/runtime-state.md`

Document the distinction between archived run-specific helpers and
reviewed reusable skill scripts.

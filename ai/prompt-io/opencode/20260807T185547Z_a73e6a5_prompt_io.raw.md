---
model: openai/gpt-5.6-sol
service: opencode
timestamp: 2026-08-07T18:55:47Z
git_ref: a73e6a5
diff_cmd: git diff HEAD~1..HEAD
---

Distilled the supplied Xonsh fork-session deletion snippet into a reusable
`opencode-cleaning` skill and OpenCode command.

> `git diff HEAD~1..HEAD -- skills/opencode-cleaning providers/opencode/commands/opencode-cleaning.md .opencode/commands/opencode-cleaning.md tests/test_opencode_cleaning.py deploy-manifest.conf README.md tests/deploy/test-deploy.sh`

Tagged OpenCode source and read-only local metadata established that UI forks
are root sessions without `parentID`; OpenCode itself generates the canonical
`(fork #N)` title. The helper uses a full anchored title pattern, exact
directory, seven-day age default, newest-session protection, and a
preview-derived approval token before deletion.

Validation: six helper tests passed; a real preview protected the active
supervisor, found zero candidates, and deleted nothing; deployment validation
reported zero errors and the full deployment suite passed 36/36.

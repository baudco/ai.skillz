---
description: Inspect and coordinate Git branches, worktrees, and stacked history.
---

Load the `git-mgmt` skill and follow it completely. Use the arguments as the
requested branch, worktree, pull request, or Git operation. Inspection does
not authorize mutation; execute a rebase, restack, merge, commit, push, branch
change, or cleanup only when the current request explicitly names it.

Git-management request from the user: $ARGUMENTS
